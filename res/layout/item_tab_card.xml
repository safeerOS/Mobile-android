<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tabCardRoot"
    android:layout_width="match_parent"
    android:layout_height="180dp"
    android:background="@drawable/bg_tab_card"
    android:padding="8dp">

    <!-- Tab Header with Favicon, Title, and Close Button -->
    <LinearLayout
        android:id="@+id/tabHeader"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_alignParentTop="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvTabFavicon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:text="🌐"
            android:textSize="13sp"
            android:gravity="center" />

        <TextView
            android:id="@+id/tvTabTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:ellipsize="end"
            android:singleLine="true"
            android:text="Zavihek"
            android:textColor="@color/text_primary"
            android:textSize="12sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/btnTabClose" android:focusable="true" android:contentDescription="@string/btn_close"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/bg_mobile_icon_button"
            android:gravity="center"
            android:text="✕"
            android:textColor="@color/text_secondary"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Tab Preview / URL Snippet -->
    <FrameLayout
        android:id="@+id/tabPreviewContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tabHeader"
        android:layout_marginTop="6dp"
        android:background="@color/bg_secondary"
        android:padding="6dp">

        <TextView
            android:id="@+id/tvTabUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="https://google.com"
            android:textColor="@color/text_dim"
            android:textSize="11sp" />

        <TextView
            android:id="@+id/tvActiveBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:background="@drawable/bg_mobile_omnibox"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp"
            android:text="AKTIVEN"
            android:textColor="@color/accent_cyan"
            android:textSize="9sp"
            android:textStyle="bold"
            android:visibility="gone" />
    </FrameLayout>

</RelativeLayout>
